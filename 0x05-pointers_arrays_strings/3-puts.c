#include <stdio.h>

/**
 * _puts - prints a string to stdout
 * @str: string to print
 *
 * Return: void
 */
void _puts(char *str)
{
int a = 0;

while (str[i] != '\0')
{
_putchar(str[i]);
a++;
}
_putchar(\'n');
}
